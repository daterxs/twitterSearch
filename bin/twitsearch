#!/usr/bin/env node

const program = require('commander');

program
  .version('0.2.0')
	.command('search', 'Commands related to mongodb')
  .command('utils', 'Differents utilities')
  .command('test', 'Testing')
  .parse(process.argv);
